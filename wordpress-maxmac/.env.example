# =============================================================================
# MaxMac WordPress — LIVE (maxmacsafaris.com)
# =============================================================================
# setup.sh writes these into wp-config.php automatically.
# This file is optional on the server; keep it for reference or tooling.
#
# On the VPS:
#   cp .env.example .env
#
# Local development only — use instead:
#   SF_FRONTEND_ORIGIN=http://localhost:4321
# =============================================================================

SF_FRONTEND_ORIGIN=https://maxmacsafaris.com

# Optional: USD → KES when running import-africanmecca-trips.php
AMS_USD_TO_KES=130
