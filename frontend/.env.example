# =============================================================================
# MaxMac frontend — LIVE (maxmacsafaris.com)
# =============================================================================
# On the VPS:
#   cp .env.example .env
#
# Local development only — use instead:
#   PUBLIC_WP_URL=http://localhost:8000
# =============================================================================

PUBLIC_WP_URL=https://wp.maxmacsafaris.com
